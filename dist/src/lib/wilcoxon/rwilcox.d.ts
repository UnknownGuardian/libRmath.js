import { IRNG } from '../rng';
export declare function rwilcox(N: number | number[], m: number, n: number, rng: IRNG): number[];
export declare function rwilcoxOne(m: number, n: number, rng: IRNG): number;
