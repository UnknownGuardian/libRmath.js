import { IRNG } from '../rng/irng';
export declare function rbinom(n: number | number[], nin: number, pp: number, rng: IRNG): number[];
export declare function rbinomOne(nin: number, pp: number, rng: IRNG): number;
