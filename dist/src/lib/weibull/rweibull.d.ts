import { IRNG } from '../rng';
export declare function rweibull(n: number | number[], shape: number, scale: number | undefined, rng: IRNG): number[];
export declare function rweibullOne(shape: number, scale: number | undefined, rng: IRNG): number;
