import { IRNG } from '../rng/irng';
export declare function rmultinom(n: number, size: number, prob: number | number[], rng: IRNG): number[];
export declare function rmultinomOne(size: number, prob: number | number[], rng: IRNG): number[];
