import { IRNG } from '../rng';
export declare function rcauchy(n: number, location: number | undefined, scale: number | undefined, rng: IRNG): number[];
export declare function rcauchyOne(location: number | undefined, scale: number | undefined, rng: IRNG): number;
