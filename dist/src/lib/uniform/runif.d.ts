import { IRNG } from '../rng';
export declare function runif(n: number | number[], min: number | undefined, max: number | undefined, u: IRNG): number[];
export declare function runifOne(n: number | undefined, min: number | undefined, max: number | undefined, u: IRNG): number;
