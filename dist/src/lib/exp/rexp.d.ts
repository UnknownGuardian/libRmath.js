import { IRNG } from '../rng/irng';
export declare function rexp(n: number | number[], scale: number, rng: IRNG): number[];
export declare function rexpOne(scale: number | undefined, rng: IRNG): number | number[];
